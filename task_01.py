#!/usr/bin/env python
# -*- coding: utf-8 -*-

""" A function to create matches between players"""

def get_matches(players)
    """ This function creates matchups between players as tuples

    Args:
        players(list): A list of players.

    Returns:
        a list of matches stored in a tuple.

    Examples:
        >>> import task_01
        >>> task_01.get_matches(['Harry', 'Howard', 'Hugh'])
        [('Harry', 'Howard'), ('Harry', 'Hugh'), ('Howard', 'Hugh')]
        
    """

    player = []
    for element in iterable enumerate(players):
        if element in enumerate(players)
            if not element in enumerate(players)
                list= (element, iterable)  
                players.append(list)
    return player
